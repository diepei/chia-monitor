[project]
name = "chia-monitor-agent"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["fastapi==0.116.1", "uvicorn[standard]==0.35.0", "httpx==0.28.1", "psutil==7.0.0", "PyYAML==6.0.2"]

[project.optional-dependencies]
test = ["pytest==8.4.1"]

[tool.pytest.ini_options]
pythonpath = ["."]
